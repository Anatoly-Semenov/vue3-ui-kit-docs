import { UiIcon } from "@/types"

export namespace UiButtonIcon {
	export type Size = "s" | "m" | "l"
	export type Type = "dark" | "icon" | "icon-dark" | "primary" | "ghost"
	export type IconName = UiIcon.Name
}
